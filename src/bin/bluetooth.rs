// use tokio_stream::StreamExt;
// #[tokio::main]
fn main() {
    /*     let ses = bluer::Session::new().await.unwrap();
    let adapter = ses.default_adapter().await.unwrap();
    let addr = adapter.address().await.unwrap();
    let dev = adapter.device(addr).unwrap();
    println!(
        "name: {:?}, address: {:?}",
        dev.name().await,
        dev.address()
    );
    let mut event_iter = dev.events().await.unwrap();
    while let Some(event) = event_iter.next().await {
        println!("event: {:?}", event);
    } */
}
